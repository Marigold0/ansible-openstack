---
- name: Restart mariadb
  ansible.builtin.systemd_service:
    name: mariadb
    state: restarted
    daemon_reload: true
  become: true

- name: Tunggu mariadb
  ansible.builtin.wait_for:
    #host: 127.0.0.1
    port: 3306
    delay: 3
    timeout: 90

